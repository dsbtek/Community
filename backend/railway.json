{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "bash -c 'python manage.py migrate && python manage.py collectstatic --noinput && uvicorn community.asgi:application --host 0.0.0.0 --port $PORT'",
        "healthcheckPath": "/api/health",
        "healthcheckTimeout": 100,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
